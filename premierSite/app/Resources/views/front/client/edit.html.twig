{% extends 'front/base.html.twig' %}

{% block body %}
<div class="container">
    <h1>Client edit</h1>

    {{ form_start(edit_form) }}
    {{ form_widget(edit_form) }}
    <input type="submit" value="Edit" class="btn" />
    <a href="{{ path('client_index') }}" class="btn">Back to the list</a>
    <a href="{{ path('client_delete', {'id': client.id, 'token': csrf_token('delete_client')}) }}" class="btn danger">Delete</a>
    {{ form_end(edit_form) }}

</div>
{% endblock %}
