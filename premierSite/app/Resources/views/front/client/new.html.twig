{% extends 'front/base.html.twig' %}

{% block body %}
    <h1>Client creation</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input class="btn success" type="submit" value="Ajouter" name="add"/>
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('client_index') }}"class="btn">Back to the list</a>
        </li>
    </ul>
{% endblock %}
